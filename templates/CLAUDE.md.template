# Claude Code {{PROJECT_NAME}} 配置

> AI 驱动的开发工作区配置

## 可用技能

技能从 `.claude/skills/` 加载（从 claude-code-java 符号链接）。

要使用技能，先加载它，然后用自然语言调用：

### 1. Git Commit 消息
**加载**：`view .claude/skills/git-commit/SKILL.md`

**使用场景**：
- "Commit staged changes"
- "Create commit for bug fix #123"
- "Generate conventional commit message"

**示例**：
```
> view .claude/skills/git-commit/SKILL.md
> "Commit these changes"
→ fix(plugin-loader): prevent NPE when directory missing
```

### 2. Test Quality (JUnit 5 + AssertJ)
**加载**：`view .claude/skills/test-quality/SKILL.md`

**使用场景**：
- "Add tests for PluginManager.loadAll()"
- "Review existing tests in PluginLoaderTest"
- "Improve test coverage for lifecycle module"

**示例**：
```
> view .claude/skills/test-quality/SKILL.md
> "Add unit tests for ExtensionFactory with edge cases"
→ 生成 JUnit 5 测试及 AssertJ 断言
```

### 3. Issue Triage
**加载**：`view .claude/skills/issue-triage/SKILL.md`

**使用场景**：
- "Triage the last 10 issues"
- "Check recent bug reports"
- "Prioritize open feature requests"

**示例**：
```
> view .claude/skills/issue-triage/SKILL.md
> "Triage issues from {{REPO_NAME}}, last 15"
→ 分类、标记、建议响应
```

## MCP 服务器（可选）

MCP 服务器通过结构化的、token 高效的操作增强功能：

| Server | 好处 |
|--------|----------|
| GitHub MCP | Issue 管理、PR 创建 |
| Filesystem MCP | 结构化文件树导航 |
| Git MCP | Commit 历史、blame、日志解析 |

要配置 MCP 服务器，从 claude-code-java 运行：
```bash
./scripts/configure-mcp.sh /path/to/this/project
```

详情见 [MCP 文档](https://modelcontextprotocol.io/)。

## 常见工作流

### 日常开发流程
```bash
# 1. 启动会话
claude code .

# 2. 开发功能/修复
# ... 进行代码更改 ...

# 3. 添加测试（加载 test-quality skill）
> view .claude/skills/test-quality/SKILL.md
> "Add tests for new functionality in class X"

# 4. 提交（加载 git-commit skill）
> view .claude/skills/git-commit/SKILL.md
> "Commit staged changes"

# 5. 推送并创建 PR
> "Push changes and create PR for issue #123"
```

### 每周维护
```bash
# 周一早上：Issue 分类
claude code .

> view .claude/skills/issue-triage/SKILL.md
> "Triage the last 20 issues, categorize and prioritize"

# 审查建议的操作
> "Apply labels and post responses as suggested"
```

### 代码审查
```bash
# 审查 PR
> "Review PR #456 focusing on:
   - Test coverage (use test-quality skill)
   - Commit message quality (use git-commit skill)
   - Code patterns and best practices"
```

## Token 预算指南

优化 token 使用：

1. **每会话加载一次技能** - 技能保持在上下文中
2. **批量操作** - 一起处理多个 issues/tests
3. **使用 MCP（如果可用）** - 比 bash 命令更高效
4. **定向文件读取** - 仅读取需要的文件

### 目标 Token 使用

| Task | 不使用技能 | 使用技能 | 节省 |
|------|----------------|-------------|---------|
| Commit message | ~800 tokens | ~300 tokens | 62% |
| 添加 3 个测试 | ~2000 tokens | ~800 tokens | 60% |
| 分类 10 个 issues | ~5000 tokens | ~2000 tokens | 60% |

## 避免事项

1. **不要重复加载技能** - 每会话加载一次
2. **不要逐个处理 issues** - 批量处理
3. **不要过度设计** - 在适当任务上使用技能
4. **不要忽略技能指南** - 它们针对 token 进行了优化

## 项目特定说明

### 构建命令
```bash
# Maven
mvn clean install
mvn test
mvn jacoco:report

# 检查测试覆盖率
open target/site/jacoco/index.html
```

### 测试策略
- 目标：核心逻辑 80%+ 覆盖率
- 重点：业务逻辑，非样板代码
- 工具：JUnit 5、AssertJ、Mockito

### 提交指南
- 遵循 Conventional Commits
- 引用 issues："Fixes #123"
- 主题保持在 50 字符以内

### Issue 管理
- 48 小时内标记所有新 issues
- 1 周内回复问题
- 关闭陈旧（>90 天无活动）issues

## 资源

- [claude-code-java](https://github.com/decebals/claude-code-java) - 技能仓库
- [Claude Code Docs](https://code.claude.com/docs) - 官方文档
- [Conventional Commits](https://www.conventionalcommits.org/) - 提交格式
- [AssertJ Docs](https://assertj.github.io/doc/) - 断言库

## 技巧

### 快速技能加载
```bash
# 添加到 shell 别名
alias cc-commit='echo "view .claude/skills/git-commit/SKILL.md"'
alias cc-test='echo "view .claude/skills/test-quality/SKILL.md"'
alias cc-triage='echo "view .claude/skills/issue-triage/SKILL.md"'
```

### 会话连续性
```bash
# 会话结束时保存上下文
> "Summarize what we worked on today for next session"

# 第二天恢复
> "Review yesterday's summary and continue"
```

### 衡量胜利
```bash
# 跟踪 token 使用
> /token usage

# 比较采用技能前后的情况
# 在团队回顾中记录节省
```

---

**最后更新**：{{DATE}}
**claude-code-java 版本**：v0.1
